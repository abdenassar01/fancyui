export type ButtonProps = {
    color?: string,
    ph?: number,
    pv?: number,
    rounding?: number,
    textColor?: string,
    text: string,
    onClick: () => void
}

export type ButtonStyles = {
    color?: string,
    ph?: number,
    pv?: number,
    rounding?: number,
    textColor?: string,
}